version: '3.8'

services:
  bot:
    build: .
    restart: always
    env_file:
      - config.env
    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    restart: always
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
